name: Build WakeGuard APK

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@v3
      - name: Install Android SDK 36
        run: sdkmanager "platforms;android-36" "build-tools;36.0.0"
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '9.5.0'
      - name: Reconstruct source archive
        run: |
          cat .wakeguard-parts/*.b64 | base64 --decode > WakeGuard-v0.5.0-source-fixed.zip
          unzip -t WakeGuard-v0.5.0-source-fixed.zip
          unzip -q WakeGuard-v0.5.0-source-fixed.zip
      - name: Apply WakeGuard v1.2.9 hybrid offline-first search
        run: |
          python3 tools/patch_v129.py
          grep -n "hasValidatedInternet\|resolveOnlineCountryHints\|onlineHintCache\|pendingOnlineSearch\|350L" WakeGuard/app/src/main/java/jp/wakeguard/alarm/ClockActivity.java
          grep -n "fastSelectableZones\|countryTermsCache\|zonesByCountry\|45L\|convertView instanceof LinearLayout" WakeGuard/app/src/main/java/jp/wakeguard/alarm/ClockActivity.java
          grep -n "オフライン検索を常に使い" WakeGuard/app/src/main/java/jp/wakeguard/alarm/I18n.java
          grep -n "ACCESS_NETWORK_STATE\|android.intent.action.PROCESS_TEXT\|android.intent.action.SEND" WakeGuard/app/src/main/AndroidManifest.xml
          grep -n "androidx.browser\|versionName\|versionCode" WakeGuard/app/build.gradle.kts
      - name: Upload patched source snapshot
        uses: actions/upload-artifact@v4
        with:
          name: WakeGuard-v1.2.9-patched-source-debug
          path: |
            WakeGuard/app/src/main/java/jp/wakeguard/alarm/*.java
            WakeGuard/app/src/main/AndroidManifest.xml
            WakeGuard/app/src/main/res/layout/*.xml
            WakeGuard/app/src/main/res/values/*.xml
            WakeGuard/app/src/main/res/xml/*.xml
            WakeGuard/app/build.gradle.kts
      - name: Build debug APK
        working-directory: WakeGuard
        run: gradle assembleDebug --stacktrace
      - uses: actions/upload-artifact@v4
        with:
          name: WakeGuard-v1.2.9-debug-apk
          path: WakeGuard/app/build/outputs/apk/debug/app-debug.apk
